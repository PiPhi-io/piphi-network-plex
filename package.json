{
  "name": "piphi-plex-widgets",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-widgets.mjs",
    "test": "node --test tests/*.test.mjs",
    "validate:widgets": "piphi-widget validate widgets/recently-added/widget.manifest.json && piphi-widget validate widgets/continue-watching/widget.manifest.json && piphi-widget validate widgets/library-search/widget.manifest.json"
  },
  "devDependencies": {
    "esbuild": "0.25.12",
    "piphi-network-widget-sdk": "0.5.0"
  }
}
